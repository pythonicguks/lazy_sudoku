[project]
name = "lazy-sudoku"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = [
    ".",
]

[tool.uv.sources.harness-mcp]
git = "https://github.com/pythonicguks/gvdm_multitools"
branch = "claude/feed-repository-files-cewjnw"
subdirectory = "harness-mcp"

[tool.harness]
schema_version = "1"
conflict_policy = "warn"

[[tool.harness.instances]]
id = "harness-mcp"
version = "0.1.0"
tools = [
    "harness.lint_check",
    "harness.format_check",
    "harness.type_check",
    "harness.test_run",
    "harness.dep_sync",
    "harness.dep_add",
    "harness.repo_status",
    "harness.repo_diff",
    "harness.ensure_gitignore",
    "harness.capitalize_session",
]
owns_paths = [
    "docs/adr",
    "docs/harness-notes.md",
    "CHANGELOG-proposals.md",
]

[dependency-groups]
dev = [
    "harness-mcp",
]
